#include <unistd.h>
#include <fcntl.h>
#include <stdio.h>

int main() {
    // char buffer[2]; 
    // buffer[0] = 'A';   // Assign 'A' to the first element
    // buffer[1] = '\n';   // Assign newline to the second element (not overwriting 'A')

    printf("test tfsvf"); // This will print 'A' followed by a newline

    return 0;
}
